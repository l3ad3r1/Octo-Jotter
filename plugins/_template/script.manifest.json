{
  "id": "your-script-id",
  "name": "Your Script Plugin",
  "version": "1.0.0",
  "minAppVersion": "1.6",
  "author": "your-github-handle",
  "description": "One-line description of your commands.",
  "type": "script",
  "permissions": [],
  "main": "octo.registerCommand(\"example\", \"Example: to UPPERCASE\", function(t){ return String(t).toUpperCase(); });\n// Add more commands with octo.registerCommand(id, name, function(text){ return newText; });\n// Needs \"permissions\": [\"notes:write\"] to call octo.notes.create(title, content).\n// Needs \"permissions\": [\"notes:read\"]  to call octo.notes.list()."
}
