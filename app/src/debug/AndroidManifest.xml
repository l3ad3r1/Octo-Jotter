<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only manifest. Merged into debug builds only; never ships in release.
  Registers the Phase 0 on-device LLM smoke-test activity as a separate launcher
  entry ("OctoJotter LLM Debug"). See docs/ON-DEVICE-AI.md.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name="com.l3ad3r1.octojotter.ai.debug.OnDeviceLlmDebugActivity"
            android:exported="true"
            android:label="OctoJotter LLM Debug">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
