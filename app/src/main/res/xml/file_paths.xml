<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Database backups written by exportDatabase() live in filesDir/exports. -->
    <files-path name="exports" path="exports/" />
    <!-- Note images and drawings inserted from the editor. -->
    <files-path name="attachments" path="attachments/" />
    <!-- Downloaded update APKs, served to the package installer. -->
    <files-path name="updates" path="updates/" />
</paths>
